# clonar repositorio

cuando estamos haciendo una búsqueda en GitHub y necesitamos clorar los repositorios directamente entonces vamos a GitHub buscamos lo que necesitamos y luego hacemos este procedimiento con el siguiente comando
```python
git clone {URL DEL REPOSITORIO}
```
 